--- Makefile.ori	2025-10-09 13:32:01.272635224 +0200
+++ Makefile	2025-10-09 13:33:46.436996010 +0200
@@ -23,18 +23,18 @@
 
 # Leave blank after "=" to disable; put "= 1" to enable
 WITH_R_PLUGINS = 1
-WITH_WEBCHECK = 1
+WITH_WEBCHECK =
 FORCE_32BIT = 
 WITH_ZLIB = 1
 WITH_LAPACK = 
-FORCE_DYNAMIC = 
+FORCE_DYNAMIC = 0
 
 # Put C++ compiler here; Windows has it's own specific version
 CXX_UNIX = g++
 CXX_WIN = c:\bin\mingw\bin\mingw32-g++.exe
 
 # Any other compiler flags here ( -Wall, -g, etc)
-CXXFLAGS = 
+CXXFLAGS = -std=gnu++14
 
 # Misc
 LIB_LAPACK = /usr/lib/liblapack.so.3
@@ -44,7 +44,7 @@
 # Do not edit below this line
 # --------------------------------------------------------------------
 
-CXXFLAGS += -O3 -I.
+CXXFLAGS += -O3 -I. 
 OUTPUT = plink
 
 # Some system specific flags
@@ -63,6 +63,7 @@
  ifndef FORCE_DYNAMIC
   CXXFLAGS += -static
  endif
+ # LIB += -lnss3
 endif
 
 ifeq ($(SYS),MAC)
