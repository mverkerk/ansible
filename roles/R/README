This role will compile, install and configure R and install packages provided in a R rda.

- Copy R source package to files dir
- Alter vars/main.yml or set vars in playbook

In order to create a rda file with packages from an existing R installation:

source("https://bioconductor.org/biocLite.R")
pkgs <- rownames(installed.packages())
save(pkgs, file="biocond_packages.rda")
